apiVersion: v1
kind: Service
metadata:
  name: radarr-service
spec:
  type: ClusterIP
  selector:
    app: radarr
  ports:
    {{- range .Values.radarr.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ default "TCP" .protocol }}
    {{- end }}